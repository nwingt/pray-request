// Mirrored in .github/workflows/prayrequest.yml's bash gate; keep them in sync.
export const SUMMON_PATTERN = /(^|\s)@prayrequest(\s|$)/i;
export const REROLL_PATTERN = /(^|\s)@prayrequest\s+reroll(\s|$)/i;
export const BOT_LOGIN_PREFIX = "prayrequest";
